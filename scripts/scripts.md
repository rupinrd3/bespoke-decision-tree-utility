# scripts Folder Overview

This folder contains platform launch and setup scripts (`run_ubuntu.sh`, `run_portable.bat`, `build_portable.bat`, `setup_ubuntu.sh`, `setup_windows.bat`). No Python modules reside here; instead, these shell and batch files automate environment setup and portable builds for the desktop utility.
